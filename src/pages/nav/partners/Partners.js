import React from 'react';
import PropTypes from 'prop-types';

const Partners = () => {
    return (
        <div>
            this is partners
        </div>
    );
};

Partners.propTypes = {};

export default Partners;